# AI Custom Settlement Formula Rules Design

## Goal

Let MCN users define settlement rules from their own business logic instead of being limited to preconfigured "complex rules". Users can describe a rule in natural language, let AI generate a controlled settlement formula, preview its financial impact, and apply it as a draft. The rule only becomes active after human approval.

This design chooses a formula/DSL approach with AI assistance, but it does not execute arbitrary user scripts. The system owns parsing, validation, execution, preview, approval, and audit.

## Current Context

The existing settlement system already has several layers:

- Project-level default settlement settings are updated through `PATCH /api/projects/:projectId/settlement-rule` and require an audit reason.
- Streamer/project payable rules are frozen into `project_streamers` when a streamer joins a project, so historical settlement is stable.
- `calculateSettlementItem` supports fixed methods, CPT/base salary, tiered hourly pricing, penalties, floor, and cap.
- Complex cost rules are a separate workflow for cost preview, imports, manual cost items, dashboards, and exports.
- Single-project reconciliation combines receivable, payable, external costs, tax, and margin checks.

The missing product loop is:

```text
natural-language business rule
  -> AI-generated safe formula
  -> system validation and simulation
  -> draft rule version
  -> human approval
  -> active rule used by settlement batches
  -> historical calculation snapshot
```

## Non-Goals

- Do not allow arbitrary JavaScript, SQL, network calls, database queries, loops, recursion, randomness, or side effects in formulas.
- Do not allow AI to directly activate rules, confirm settlement batches, lock settlement, reopen locked batches, change historical batch items, or confirm payment.
- Do not expose internal receivable, margin, tax, other streamer rules, or cost formulas to streamer-facing APIs.
- Do not replace existing fixed settlement rules in one migration. Existing rules remain the fallback.
- Do not recalculate locked historical settlement batches after a rule changes.

## Recommended Approach

Use a controlled formula DSL plus AI-assisted drafting.

Users talk to AI in natural language. AI returns:

- a formula in the allowed DSL,
- a Chinese explanation,
- test cases,
- expected inputs,
- safety notes.

The backend then parses and validates the formula. If valid, the user can apply it as a draft and run simulation. Only an owner or ops manager can approve the rule version and make it active.

This gives the user real customization while keeping settlement deterministic, explainable, auditable, and testable.

## Rule Architecture

Add immutable rule versions in a new `custom_settlement_rule_versions` table instead of directly replacing `projects.default_settlement_rule`.

Each version stores:

- `scope`: `receivable`, `payable`, `external_cost`, or `reconciliation`.
- `target_type`: `project`, `project_streamer`, or `streamer_group`.
- `target_id`: nullable for project-level defaults.
- `formula`: original formula text.
- `compiled_ast`: parsed and normalized AST.
- `variables`: allowed variable list for this rule.
- `explanation`: human-readable explanation generated by AI and accepted by the user.
- `test_cases`: generated and user-adjustable test cases.
- `simulation_summary`: preview output summary.
- `status`: `draft`, `pending_review`, `active`, or `archived`.
- `effective_from`: first effective timestamp.
- `created_by`, `approved_by`, `ai_draft_id`, `reason`.

Constraints:

- Only one active version can exist for the same `project_id + scope + target_type + target_id`.
- Active versions are immutable. Changes create a new version.
- Versions are archived, not deleted.
- A rule may be active for future batches only. Locked batches are never recalculated.

## AI Interaction Flow

In project settlement settings, users open "AI build settlement rule" and enter a business rule, for example:

```text
这个项目按系统直播时长结算。绿证据全额，黄证据按 80%，红证据不计。
单场不足 60 分钟不结算；超过 3 小时的部分按 120 元/小时。
如果主播等级是 S，每场额外奖励 50 元。
```

The flow:

1. AI generates a DSL formula, explanation, and test cases.
2. The server validates the result. AI output is never trusted as executable.
3. The system simulates the new rule against recent authorized settlement-pool samples.
4. The user clicks "应用为草稿并试算"; this creates a draft only.
5. The user submits the draft for review.
6. Owner or ops manager approves activation with a required reason.

The UI must avoid a button named "立即生效" for AI-generated output. The first action is "应用为草稿并试算"; activation is a separate reviewed action.

AI can:

- suggest formulas,
- explain rules,
- produce test cases,
- identify missing inputs,
- warn about risky outcomes,
- create draft versions.

AI cannot:

- activate a rule,
- bypass approval,
- lock or confirm settlement,
- change immutable historical evidence,
- perform money movement or payment confirmation.

## Formula DSL

The DSL is a deterministic expression language for money calculation and settlement checks.

Example:

```text
payable =
  if(system_minutes < 60, 0,
    tiered(system_minutes, [
      { upto: 180, rate_per_hour: 8000 },
      { upto: null, rate_per_hour: 12000 }
    ])
    * evidence_multiplier(evidence_level, { green: 1, yellow: 0.8, red: 0 })
    + if(streamer_level == "S", 5000, 0)
  )
```

Amounts in the DSL are cents. Rates are basis points where applicable.

### Allowed Variable Groups

Live evidence:

- `system_minutes`
- `screenshot_minutes`
- `settlement_minutes`
- `evidence_level`
- `time_source`
- `views`
- `approved_at`

Project and streamer:

- `project_id`
- `project_tags`
- `streamer_id`
- `streamer_level`
- `streamer_source`
- `collaboration_id`
- `base_hourly_rate`
- `base_salary`
- `cps_rate_bps`

Business result:

- `sales_amount`
- `orders_count`
- `gift_amount`
- `supplier_fee`
- `traffic_cost`
- `manual_adjustment`

Reconciliation:

- `receivable_amount`
- `payable_amount`
- `external_cost_amount`
- `tax_amount`
- `gross_margin`
- `margin_rate_bps`

### Allowed Functions

```text
if(condition, when_true, when_false)
min(a, b)
max(a, b)
clamp(value, floor, cap)
round_money(value)
tiered(minutes, [{ upto, rate_per_hour }])
percent(amount, bps)
evidence_multiplier(evidence_level, { green, yellow, red })
in(value, ["A", "B"])
contains(tags, "tag")
```

For reconciliation rules, add check helpers:

```text
block_if(condition, message)
warn_if(condition, message)
pass_if(condition, message)
```

### Restrictions

- No user-defined functions in the first version.
- No loops, recursion, async behavior, random values, network calls, database reads, file access, or global state.
- Formula complexity is capped by AST node count, expression depth, function count, and serialized size.
- Output must be finite and non-negative for money rules unless a rule type explicitly allows signed adjustments.
- Missing variables fail validation or simulation. Production batch generation must not silently substitute arbitrary defaults.

## Rule Scopes

### Receivable Rules

Calculate what the MCN charges the customer or vendor. This can cover CPT, base fee plus CPT, GMV/CPS income, floors, caps, evidence discounts, or project-specific bonuses.

For receivable batches, project-level base fees apply once per batch, not once per streamer.

### Payable Rules

Calculate what the MCN pays a streamer. Resolution order:

1. active project-streamer payable custom rule,
2. active project-level payable custom rule,
3. frozen `project_streamers` fixed rule,
4. manual fallback.

### External Cost Rules

Generate project cost items from supplier, traffic, platform, sample, replay, or other imported fields. External cost outputs default to review-required cost items and do not automatically enter confirmed cost totals unless approved.

### Reconciliation Rules

Return `pass`, `warn`, or `block` checks for project settlement. Examples:

```text
block_if(margin_rate_bps < 1000, "毛利率低于 10%")
warn_if(red_evidence_count > 0, "存在红证据场次")
```

Reconciliation rules gate confirmation and lock flows but do not directly mutate settlement batch records.

## Data Model

### `custom_settlement_rule_versions`

Stores the approved business rule versions.

Key fields:

- `id uuid primary key`
- `organization_id uuid not null`
- `project_id uuid not null`
- `scope text not null`
- `target_type text not null`
- `target_id uuid null`
- `version_no integer not null`
- `status text not null`
- `formula text not null`
- `compiled_ast jsonb not null`
- `variables jsonb not null`
- `explanation text not null`
- `test_cases jsonb not null default '[]'`
- `simulation_summary jsonb not null default '{}'`
- `effective_from timestamptz null`
- `created_by uuid null`
- `approved_by uuid null`
- `ai_draft_id uuid null`
- `reason text not null`
- `created_at timestamptz not null default now()`
- `approved_at timestamptz null`
- `archived_at timestamptz null`

### `ai_settlement_rule_drafts`

Stores AI generation traceability.

Key fields:

- `id uuid primary key`
- `organization_id uuid not null`
- `project_id uuid not null`
- `user_prompt text not null`
- `ai_response jsonb not null`
- `generated_formula text not null`
- `generated_explanation text not null`
- `generated_test_cases jsonb not null default '[]'`
- `model text not null`
- `safety_flags jsonb not null default '[]'`
- `created_by uuid null`
- `created_at timestamptz not null default now()`

### `settlement_formula_simulations`

Stores simulation summaries.

Key fields:

- `id uuid primary key`
- `rule_version_id uuid not null`
- `sample_source text not null`
- `old_total_amount_cents bigint not null`
- `new_total_amount_cents bigint not null`
- `delta_amount_cents bigint not null`
- `largest_deltas jsonb not null default '[]'`
- `warnings jsonb not null default '[]'`
- `created_by uuid null`
- `created_at timestamptz not null default now()`

### Settlement Item Snapshot

Extend `settlement_batch_items.evidence_snapshot` with a `ruleEngine` object:

```json
{
  "ruleEngine": {
    "mode": "custom_formula",
    "ruleVersionId": "...",
    "formulaHash": "...",
    "inputs": {
      "system_minutes": 180,
      "evidence_level": "green",
      "streamer_level": "S"
    },
    "outputs": {
      "base": 24000,
      "bonus": 5000,
      "final": 29000
    },
    "explanation": "系统计时 180 分钟，按 80 元/小时计 240 元，S 级奖励 50 元。"
  }
}
```

This snapshot is required so future rule changes do not erase historical settlement explanations.

## Backend Design

Add the following modules:

- `features/settlements/custom-rule-types.ts`
- `features/settlements/custom-rule-parser.ts`
- `features/settlements/custom-rule-validator.ts`
- `features/settlements/custom-rule-engine.ts`
- `features/settlements/custom-rule-service.ts`
- `features/settlements/custom-rule-repository.ts`
- `features/settlements/custom-rule-ai.ts`

The parser converts formula text into an AST. The validator checks syntax, variable allowlists, function allowlists, money units, output ranges, and complexity. The engine executes the AST as a pure function.

The engine must not access Supabase, the filesystem, network, environment variables, timers, or random state. Database access belongs to the service/repository layer before building the execution context.

## Settlement Execution

During settlement batch generation:

1. Determine scope from `batchType`.
2. Resolve active custom rule version.
3. If a custom rule exists, build the formula input context from approved report, project, streamer, imported business data, and role-safe fields.
4. Execute the custom rule.
5. Persist amount, breakdown, explanation, formula hash, rule version ID, and input/output snapshot.
6. If no custom rule exists, keep using existing `calculateSettlementItem`.

If custom rule execution fails:

- In draft or simulation, return validation errors to the UI.
- In production batch generation, block generation. Do not silently fall back, because the user expects the active rule to be honored.
- Fixed-rule fallback is used only when no active custom rule exists.

## Amount Unit Strategy

The new custom rule engine uses cents internally. UI displays yuan.

The existing settlement engine has legacy yuan-based outputs in some paths, while complex cost and tax use cents. Implementation must introduce explicit adapters:

- legacy settlement engine output yuan -> custom/reconciliation cents,
- DSL cents -> settlement batch persisted unit,
- cost/tax cents remain unchanged,
- tests cover 100x conversion risks.

No new formula rule should store ambiguous "amount" fields without a unit suffix in persisted JSON.

## Frontend Design

In project detail -> settlement settings, add "自定义公式规则".

The page includes:

- Rule type selector: receivable, payable, external cost, reconciliation.
- Target selector: project default, streamer group, specific project streamer.
- AI chat/natural-language prompt area.
- Formula editor with syntax highlighting and server-side validation messages.
- Explanation panel.
- Test case panel.
- Simulation comparison:
  - current rule total,
  - new rule total,
  - delta,
  - largest changed streamers or reports,
  - triggered conditions,
  - margin impact.
- Risk panel:
  - negative margin,
  - abnormal increase,
  - red evidence priced,
  - missing variables,
  - unresolved imported fields.
- Version panel:
  - active version,
  - drafts,
  - pending review,
  - archived versions,
  - approver and reason.

Primary buttons:

- "AI 生成规则"
- "校验公式"
- "试算"
- "应用为草稿并试算"
- "提交审核"
- "确认生效"
- "归档规则"

Streamer-facing UI only receives personal settlement explanation summaries. It must not expose formulas, receivable rules, internal margin, tax, cost, or other streamers' data.

## API Design

Add routes:

- `POST /api/projects/:projectId/settlement-rules/ai-draft`
  - Creates an AI draft from natural language.
- `POST /api/projects/:projectId/settlement-rules/validate`
  - Validates formula and returns AST/errors.
- `POST /api/projects/:projectId/settlement-rules/simulate`
  - Runs a preview against selected samples.
- `GET /api/projects/:projectId/settlement-rules`
  - Lists active, draft, pending, archived versions.
- `POST /api/projects/:projectId/settlement-rules`
  - Creates a draft version.
- `POST /api/projects/:projectId/settlement-rules/:ruleVersionId/submit`
  - Moves draft to pending review.
- `POST /api/projects/:projectId/settlement-rules/:ruleVersionId/approve`
  - Activates the version and archives the prior active version.
- `POST /api/projects/:projectId/settlement-rules/:ruleVersionId/archive`
  - Archives a draft or active rule with reason.

All write routes require billing write access for settlement, organization scoping, role checks, and audit reasons for high-risk actions.

## Permissions

- `owner`: create, simulate, submit, approve, activate, archive.
- `ops_manager`: create, simulate, submit, approve, activate, archive.
- `finance`: view, simulate, comment or request changes; no direct activation.
- `operator_business`: create AI drafts and simulations; no activation.
- `streamer`: no access to internal rules; can view personal settlement explanation summaries.

Activation, archive, and any change affecting active formulas are high-risk settlement actions and require audit reason.

## Error Handling

Validation errors should be specific:

- unknown variable,
- unknown function,
- wrong argument count,
- unit mismatch,
- formula too complex,
- missing required input,
- possible negative output,
- output exceeds configured safety cap,
- red evidence is being paid when policy blocks it,
- simulation sample unavailable.

Production settlement generation blocks on active custom-rule failure. The response should identify the rule version and safe error category without leaking unauthorized data.

AI generation can fail independently of formula validation. In that case, keep the user's prompt and show a retryable AI error; do not create a rule version.

## Audit and Governance

Every draft application, submission, approval, archive, and failed activation attempt writes audit records.

Audit records include:

- actor,
- role,
- project,
- rule scope,
- version number,
- before/after status,
- formula hash,
- simulation delta summary,
- reason,
- AI draft ID where applicable.

AI prompt and response are retained in `ai_settlement_rule_drafts` for traceability, but the active rule version is the legal calculation source inside the product.

## Testing Plan

Unit tests:

- parser accepts valid formulas and rejects illegal syntax,
- validator rejects unknown variables/functions and excessive complexity,
- engine computes CPT, tiers, evidence discounts, bonuses, floors, caps, CPS, and signed adjustments correctly,
- amount unit adapters prevent yuan/cents 100x errors.

Service tests:

- AI draft cannot become active directly,
- one active rule per target is enforced,
- approval archives old active versions,
- role permissions match the matrix,
- production generation blocks on active custom-rule failure,
- no active custom rule falls back to existing fixed rules.

Route tests:

- unauthenticated and unauthorized requests fail,
- billing write guard blocks writes,
- validation/simulation/draft/submit/approve/archive routes map errors correctly,
- audit reason is required for high-risk transitions.

Integration/regression tests:

- payable batch uses project-streamer custom rule first,
- payable batch falls back to frozen `project_streamers` rule when no custom rule exists,
- receivable batch uses project-level custom rule,
- item snapshots include rule version, formula hash, inputs, outputs, and explanation,
- locked historical batches do not change after a rule update,
- reconciliation warns or blocks based on custom checks.

UI tests:

- AI output appears as formula plus explanation, not direct activation,
- "应用为草稿并试算" creates a draft,
- approval requires reason,
- simulation shows current/new/delta amounts and risk warnings,
- streamer view receives only personal explanation summaries.

## Rollout Plan

Phase 1: DSL core and read-only simulation.

- Add parser, validator, engine, tests.
- Add AI draft generation behind a feature flag.
- Add simulation route and UI preview.

Phase 2: Drafts and approval.

- Add rule version tables, RLS, repositories, services, and routes.
- Add draft, submit, approve, archive workflows.
- Add audit and version UI.

Phase 3: Payable and receivable execution.

- Integrate active custom rules into settlement batch generation.
- Persist item snapshots.
- Keep fixed-rule fallback.

Phase 4: External costs and reconciliation.

- Add external cost rule execution into cost-item draft flow.
- Add custom reconciliation checks into confirmation/lock gates.

Phase 5: Hardening.

- Add more sample-generation modes.
- Add formula templates.
- Add AI revision suggestions for failed validation.
- Add export fields for rule explanations.

## Implementation Defaults

Use these defaults for the first implementation plan:

- Formula authoring starts with text editing, syntax highlighting, server-side validation, and examples. Do not build a full block editor in the first version.
- Operators can create AI drafts and simulations. Only owner and ops manager can submit a draft to pending review, approve activation, or archive an active rule.
- Use scope-specific variable allowlists. Receivable, payable, external cost, and reconciliation rules should not share one global variable surface.
- External cost formulas create `draft` cost items by default. A separate confirmation action is required before those cost items count in project reconciliation.
- Set formula complexity limits in configuration, with conservative initial defaults: serialized formula under 16 KB, AST depth under 20, AST nodes under 300, and no single calculated money output above the project-level safety cap unless owner force approval is recorded.
- Require owner force reason for formulas whose simulation creates negative margin, abnormal total increase, red-evidence payment, or a missing-data fallback that would materially affect settlement.
